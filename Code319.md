# Bulb Switcher

* 对于一个非平方数来说，其因式分解(仅指拆分成两个数的积)的总数一定是偶数个，因为对于每一个分解可以调换位置，也就是说到这个数一共有偶数中情况，因此为灭的
* 对于一个平方数来说，其因式分解的总数一定是奇数，因为平方那项无法调换位置，因此为亮的

```Java
	public int bulbSwitch(int n) {
        return (int)Math.sqrt(n);
    }
```